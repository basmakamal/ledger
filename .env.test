# define your env variables for the test env here
KERNEL_CLASS='Ledger\Kernel'
APP_SECRET='$ecretf0rt3st'

# The suite runs on SQLite locally so it needs no services; CI runs the same
# suite again against PostgreSQL to prove the event store SQL stays portable.
DATABASE_URL="sqlite:///%kernel.project_dir%/var/test.db"
