# yaml-language-server: $schema=../vendor/symfony/dependency-injection/Loader/schema/services.schema.json

# This file is the entry point to configure your own services.
# Files in the packages/ subdirectory configure your dependencies.
# See also https://symfony.com/doc/current/service_container/import.html

# Put parameters here that don't need to change on each machine where the app is deployed
# https://symfony.com/doc/current/best_practices.html#use-parameters-for-application-configuration
parameters:

services:
    # default configuration for services in *this* file
    _defaults:
        autowire: true      # Automatically injects dependencies in your services.
        autoconfigure: true # Automatically registers your services as commands, event subscribers, etc.

    # makes classes in src/ available to be used as services
    # this creates a service per class whose id is the fully-qualified class name
    Ledger\:
        resource: '../src/'
        exclude:
            - '../src/Domain/'
            - '../src/Kernel.php'

    # Handlers stay plain invokables so the application layer never imports
    # the framework; Messenger learns about them here instead of via attributes.
    Ledger\Application\Wallet\Handler\:
        resource: '../src/Application/Wallet/Handler/'
        tags:
            - { name: messenger.message_handler, bus: command.bus }

    Ledger\Application\Port\Clock: '@Ledger\Infrastructure\Time\SystemClock'
    Ledger\Application\Port\EventStore: '@Ledger\Infrastructure\EventStore\DbalEventStore'
    Ledger\Domain\Wallet\WalletRepository: '@Ledger\Infrastructure\Repository\EventSourcedWalletRepository'

    # Grows as aggregates are added; keeping it explicit means a renamed class
    # never silently orphans events already written to the stream.
    Ledger\Infrastructure\EventStore\EventTypeMap:
        arguments:
            $map:
                wallet.opened: 'Ledger\Domain\Wallet\Event\WalletOpened'
                wallet.funds_deposited: 'Ledger\Domain\Wallet\Event\FundsDeposited'
                wallet.funds_withdrawn: 'Ledger\Domain\Wallet\Event\FundsWithdrawn'

    # add more service definitions when explicit configuration is needed
    # please note that last definitions always *replace* previous ones
